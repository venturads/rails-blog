<div class="post">
    <div class="text-center col-sm-7 col-sm-offset-3" id="posts">
        <div class="thumbnail">
            <a href="<%= post_url(post) %>"><%= image_tag post.image(:medium), :class => 'img-responsive' %></a>
            <% @comments.each do |comment| -%>
    <p>
        <%= comment.content %>
    </p>
<% end -%>
        </div>
    </div>
</div>

