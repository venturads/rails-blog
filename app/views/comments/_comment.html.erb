<hr>
<%= comment.content %>
<% if @post.user.id == current_user.id %>
    <%= link_to '', 
                post_comment_path(@post, comment), 
                method: :delete,
                data: { confirm: 'Are you sure?' },
                :class => 'fa fa-trash'
                %>
<% else %>
            <br><br>
        
<% end %>